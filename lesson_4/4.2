my_list = []
counter = 0
if len(my_list) > 0:
    for i in range(0, len(my_list)):
        if i % 2 == 0:
            counter += my_list[i]
    print(counter*my_list[-1])
elif len(my_list) == 0:
    print(0)
