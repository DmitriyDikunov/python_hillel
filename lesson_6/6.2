seconds = int(input())
if 8640000 >= seconds >= 0:
    minutes = divmod(seconds, 60)
    hours = divmod(minutes[0], 60)
    days = divmod(hours[0], 24)
    print(f'{days[0]} дні, {days[1]:02}:{hours[1]:02}:{minutes[1]:02}')
else:
    print(False)
